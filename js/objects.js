// Objects 

// Everything in JavaScript is an object with 3 small exceptions (Strings, numbers, and booleans) .center
// Dates, Maths, Regular Expressions, Arrays, Functions, object.center

//Objects are variables

// Objects have primitive value and primitive data type
// Primitive values are not objects and have
// Primitive data type is the type of data.

/*var myObject = {
    // dataType:value
    name: 'tom',
    age: 57,
    isMarried:false
    displayFullName: function(){
        return this.firstName + ' ' + this.lastName
    }
};

var name = myObject.displayFullName;
console.log(name.toString());

var person = {
    fName: '',
    lName: '',
    Age:15
}
*/
