// Operators 

/* 
    Operators in JavaScript

    Assignment - used to assign value to variable Ex. =, +=, -=, *=, /=, %=, **=, Ex. =, += var x = 5;
    Arithmetic - used to perform arithmetic on numbers. +, -, *, /, %, ++, --
    String Operators - use to concatenate strings +, +=
    Comparison - use to compare the difference between variable value. ==, ===, !=, !==, <, >, >=, <=, ?.
    Logical - !, &&, ||
    Bitwise
*/

//var selectedFruit;

//function eatFruit(fruit){
    //if(fruit == "apple"){
        //console.log('eat fruit!')
    //}
    //else{
        //console.log("I don't eat \'"+ fruit +"\'!")
    //}
//}

//eatFruit('orange');

/*var a = true;
var b = false;

if (a && b == 5){
    c = true;
}
else if(b == true && a == false){
    c = false;
}
else {
    c = true;
}

a and b are true*/
